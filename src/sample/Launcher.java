import sample.RampCounter;

/**
 * Launcher which serves as an entry point to the app.
 * In order to get a working, assembled program, we can NOT have the main class extend from Application.
 */
public class Launcher {

    public static void main(String[] args) {
        RampCounter.main(args);
    }
}
